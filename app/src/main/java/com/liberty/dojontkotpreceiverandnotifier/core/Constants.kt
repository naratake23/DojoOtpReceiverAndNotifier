package com.liberty.dojontkotpreceiverandnotifier.core

object PreferenceNames {
    const val FILE = "settings"
}

object PreferenceKeys {
    const val LAST_CODE = "last_code"
}

object NotifChannel {
    const val CHANNEL_ID = "otp_channel"
    const val CHANNEL_NAME = "Dojo OTP"
    const val CHANNEL_DESC = "Alerts when there is a new code available"
}

object WorkerKeys{
    const val KEY_CODE = "otp_code"
}